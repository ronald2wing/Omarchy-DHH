import QtQuick

// 1px full-width hairline divider. Color is caller-supplied (X's solid
// #2F3336). Shared by the history and results blocks to separate them from
// the content above.
Rectangle {
  height: 1
  width: parent.width
}
